# Subject 1 — Character

Humanoid figure (asymmetric front/back). Expected views (1024×1024 PNG):

- `front.png`  — 0°
- `right.png`  — 90°
- `back.png`   — 180°
- `left.png`   — 270°

Uniform white-background studio lighting; no harsh shadows.

Ground-truth scan (optional but recommended) at `ground_truth.glb`
for Hausdorff distance scoring; if absent, geometric accuracy
falls back to manual visual score.
